# Оригиналы иллюстраций

Полноразмерные исходники (PNG и т.п.). Сайт использует сжатые копии в `docs/illustrations/` (WebP).

Пересобрать веб-версии:

```bash
python3 scripts/optimize_images.py
```

Эту папку лучше не коммитить в git (см. `.gitignore`) — храните локально или в отдельном бэкапе.
